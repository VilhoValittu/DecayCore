[package]
name = "decaycore-scoring"
version = "0.2.0"
edition = "2021"

[lib]
name = "decaycore_scoring"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
